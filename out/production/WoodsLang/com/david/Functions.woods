// A function to print Fibonacci numbers up to a user inputted number
fun fib(n)
{
    if (n <= 1) {
        return n;
    }
    return fib(n - 2) + fib(n - 1);
}

var fibNum = double(input("Enter the number of Fibonacci numbers you want: "));

for (var i = 0; i < fibNum; i = i + 1)
{
    print fib(i);
}