var current = 0;
var temp;

var fibNum = double(input("Enter the number you want to go up to: "));

// Fibonacci sequence from 0 to inputted number
for (var b = 1; current < fibNum; b = temp + b)
{
    // Print out current Fibonacci number
    print current;

    // Set temporary value to current value
    temp = current;

    // Update current value
    current = b;
}